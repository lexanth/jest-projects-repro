{
  "name": "jest-projects-repro",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-env": "^7.12.1",
    "@babel/preset-react": "^7.12.5",
    "jest": "^26.6.3",
    "prettier": "^2.1.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:ci": "USE_SIMPLIFIED_CACHE=true jest --runInBand",
    "clean": "jest --clearCache"
  }
}
